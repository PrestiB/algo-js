/*Average

function average(arr) {
  let i = 0,
    sum = 0,
    ArrayLen = arr.length;
  while (i < ArrayLen) {
    sum = sum + arr[i++];
  }
  return sum / ArrayLen;
}
let arr = [1, 2, 3, 4, 5];
let a = average(arr);
console.log(a);*/

/*Min

function min(arr) {
  let a = arr.length;
  let minimum = arr[a - 1];
  while (a--) {
    if (arr[a] < minimum) {
      minimum = arr[a];
    }
  }
  return minimum;
}
let array = [1, 2, 3, 4, 5];
let m = min(array);
console.log(m);*/

/*Max

function max(arr) {
  let a = arr.length;
  maximum = arr[a - 1];
  while (a--) {
    if (arr[a] > maximum) {
      maximum = arr[a];
    }
  }
  return maximum;
}
let array = [1, 2, 3, 4, 5];
let m = max(array);
console.log(m);*/

/*Bonus*/

